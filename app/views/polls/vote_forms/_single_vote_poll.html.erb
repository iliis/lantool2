Choose one: <br>

<%= form_for @poll, :url => {:action => :receive_vote } do |f| %>
	<% @poll.options.each do |o| %>
		<%= radio_button_tag :vote, o.id %> <%= label_tag "vote_"+o.id.to_s, o.text %> <br >
	<% end %>
	<br />
	<%= f.submit 'abstimmen' %>
<% end %>
