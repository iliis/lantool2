Choose some: <br>

<%= form_for @poll, :url => {:action => :receive_vote } do |f| %>
	<% @poll.options.each do |o| %>
		<%= check_box_tag 'vote[]', o.id, false, :id => 'vote_'+o.id.to_s %> <%= label_tag "vote_"+o.id.to_s, o.text %> <br >
	<% end %>
	<br />
	<%= f.submit 'abstimmen' %>
<% end %>
