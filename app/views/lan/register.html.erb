<% content_for :site_title do %>Anmeldung: LAN, <%= @short_descr %><% end %>
<% content_for :page_title do %>Anmeldung LAN<% end %>
<% content_for :page_subtitle do if @lan %>
<%= link_to "Teilnehmer: #{@lan.attendances.count}", :action => 'participants' %>
<% end end %>

<%= image_tag 'img.jpg', :alt => "September '12" %>
<br />

<% if @lan %>
  <%= @lan.description.html_safe %>
  
  <hr>

  <% if @lan.registration_open %>
    <%= render 'register_form' %>
  <% else %>
    <% if Time.current < @lan.starttime %>
      Sorry, leider kannst du dich noch nicht anmelden. <br />
      Du kannst dich allerdings
      <%= link_to 'in die Mailingliste eintragen', :controller => 'mailinglists', :action => 'new' %>,
      um eine Einladung zu bekommen, sobald die Anmeldung offen ist.
    <% else %>
      Sorry, leider kannst du dich nicht mehr anmelden. <br />
      Du kannst dich allerdings
      <%= link_to 'in die Mailingliste eintragen', :controller => 'mailinglists', :action => 'new' %>,
      um eine Einladung für die nächste LAN zu bekommen.
    <% end %>
  <% end %>

<% else %>
  Es ist gerade keine LAN geplant.
  Falls du informiert werden möchtest, wenn eine organisiert wird, kannst du dich
  <%= link_to 'in die Mailingliste eintragen', :controller => 'mailinglists', :action => 'new' %>.
<% end %>

